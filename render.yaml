services:
    - type: web
    name: telegram-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn webhook:app  # <- webhook.py me app hai isliye
    envVars:
            - key: BOT_TOKEN
        value: YOUR_BOT_TOKEN
